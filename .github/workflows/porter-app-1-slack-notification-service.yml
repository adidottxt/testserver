"on":
    push:
        branches:
            - dev
name: Deploy to slack-notification-service
jobs:
    porter-deploy:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v4
            - name: Set Github tag
              id: vars
              run: echo "sha_short=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT
            - name: Setup porter
              uses: porter-dev/setup-porter@v0.1.0
            - name: Deploy stack
              timeout-minutes: 30
              run: porter apply
              env:
                PORTER_APP_NAME: slack-notification-service
                PORTER_CLUSTER: "1"
                PORTER_DEPLOYMENT_TARGET_ID: 5ba191d5-76e7-4c28-a372-76c8277384a8
                PORTER_HOST: https://adi.withporter.run
                PORTER_PROJECT: "1"
                PORTER_TAG: ${{ steps.vars.outputs.sha_short }}
                PORTER_TOKEN: ${{ secrets.PORTER_APP_1_1107919640 }}
